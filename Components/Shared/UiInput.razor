@using Microsoft.AspNetCore.Components

<div class="mb-3 position-relative">
    @if (!string.IsNullOrEmpty(Label))
    {
        <label class="small-label mb-1 d-block">@Label</label>
    }
    <div class="position-relative">
        @if (!string.IsNullOrEmpty(Icon))
        {
            <i class="bi @Icon position-absolute top-50 translate-middle-y text-muted small" 
               style="inset-inline-start: 12px;"></i>
        }
        <input type="@Type" 
               class="input-hrms w-100 @(string.IsNullOrEmpty(Icon) ? "" : "ps-5") @DirectionClass @CssClass" 
               style="@(string.IsNullOrEmpty(Icon) ? "" : "padding-inline-start: 36px;")"
               placeholder="@Placeholder" 
               value="@Value" 
               @oninput="OnInput"
               @onchange="OnChange"
               disabled="@Disabled"
               aria-label="@AriaLabel" />
    </div>
</div>

@code {
    [Parameter] public string Value { get; set; } = "";
    [Parameter] public EventCallback<string> ValueChanged { get; set; }
    [Parameter] public string Label { get; set; } = "";
    [Parameter] public string Placeholder { get; set; } = "";
    [Parameter] public string Type { get; set; } = "text";
    [Parameter] public string Icon { get; set; } = "";
    [Parameter] public string CssClass { get; set; } = "";
    [Parameter] public string AriaLabel { get; set; } = "";
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public bool IsRtl { get; set; } = false;

    private string DirectionClass => IsRtl ? "text-end" : "";

    private async Task OnInput(ChangeEventArgs e)
    {
        Value = e.Value?.ToString() ?? "";
        await ValueChanged.InvokeAsync(Value);
    }

    private async Task OnChange(ChangeEventArgs e)
    {
        Value = e.Value?.ToString() ?? "";
        await ValueChanged.InvokeAsync(Value);
    }
}
